spring:
  application:
    name: eureka-server

  cloud:
    config:
      profile: native
      uri: http://localhost:8888

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  instance:
    hostname: localhost